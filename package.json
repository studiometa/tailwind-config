{
  "name": "@studiometa/tailwind-config",
  "version": "1.0.0-alpha.9",
  "description": "A custom Tailwind CSS config",
  "main": "src/index.js",
  "scripts": {
    "test": "npm run lint && npm run test:build && echo \"\\033[0;37mAll test passed! 🎉\n\" && exit 0",
    "test:build": "if npm run style:build 2>&1 >/dev/null | tee tmp | grep -q 'Error'; then cat tmp && rm tmp && exit 1; else rm tmp && exit 0; fi;",
    "style:build": "rm -rf tests/dist && parcel build tests/src/*.css --out-dir tests/dist --no-cache --no-minify --no-source-maps",
    "lint": "eslint src/",
    "docs:dev": "vuepress dev docs",
    "docs:build": "vuepress build docs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/studiometa/tailwind-config.git"
  },
  "author": "Studio Meta <agence@studiometa.fr> (https://www.studiometa.fr)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/studiometa/tailwind-config/issues"
  },
  "homepage": "https://github.com/studiometa/tailwind-config#readme",
  "devDependencies": {
    "@studiometa/eslint-config": "^2.0.3",
    "@studiometa/prettier-config": "^2.0.0",
    "eslint": "^6.8.0",
    "flat": "^5.0.0",
    "fuzzysort": "^1.1.4",
    "parcel": "^1.12.4",
    "prettier": "^1.19.1",
    "tailwindcss": "^1",
    "to-string-loader": "^1.1.6",
    "vuepress": "^1.2.0"
  },
  "dependencies": {
    "lodash.max": "^4.0.1",
    "lodash.range": "^3.2.0",
    "tailwindcss-typography": "^3.0.0"
  },
  "peerDependencies": {
    "tailwindcss": "^1"
  },
  "engines": {
    "node": ">=8.3.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
